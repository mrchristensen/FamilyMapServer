Todo:

Make tests (including AuthTokenDoa tests.)
Recomment Event and User tests
Logger instead of print
make genGenerations static?
Rename Generator
Figure out exceptions (especially with atabase and DOA stuffs)
Standardize userNAme and data base vs database
Java docs for everything?
All other TODOS (that are in the code)
read through spec
simplify person services because all strings can be null (you don't need to funkery)
move verification to DAO (not in the individual functions)
clean up comments
make sure register calls fill