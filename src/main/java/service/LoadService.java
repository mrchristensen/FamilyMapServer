package service;

/**
 * Handles the load functionality
 */
public class LoadService {
    /**
     * Calls the clear service and then loads the data
     * @param userName the username of the current user
     */
    void load(String userName){
    }
}
